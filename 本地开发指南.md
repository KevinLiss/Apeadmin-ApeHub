---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: '7b23d958-269a-4afd-a2ef-f22c4a2edcd7'
  PropagateID: '7b23d958-269a-4afd-a2ef-f22c4a2edcd7'
  ReservedCode1: '52543e97-d388-4b2a-8d48-8271baee748c'
  ReservedCode2: '52543e97-d388-4b2a-8d48-8271baee748c'
---

# apeadmin-dev 本地开发基座使用说明

本目录是**独立克隆的 ApeAdmin Python 版基座**，用于官网插件（apehub_web）日常开发。
不再在 `TeleAgent的工作空间/apeadmin/` 目录内开发插件，避免嵌套 git 仓库在提交时互相污染。

## 目录结构（三层 git，互不嵌套跟踪）

| 层级 | 路径 | git 仓库 | 作用 |
|---|---|---|---|
| 外层 | `官网demo/` | gitcode apeuidemo（远程暂 403） | 官网页面原型、方案文档 |
| 基座 | `官网demo/apeadmin-dev/` | github KevinLiss/ApeAdmin | ApeAdmin 底座（只读为主，勿乱提交） |
| 插件 | `官网demo/apeadmin-dev/backend/src/plugins/builtin/apehub_web/` | gitcode apehub | 官网门户插件（日常开发主战场） |

外层 `.gitignore` 已排除 `apeadmin-dev/`；基座 `.gitignore` 已排除 `apehub_web/` 插件目录。

## 日常开发流程

### 1. 启动后端（端口 8300）

```bash
cd 官网demo/apeadmin-dev/backend
env NO_PROXY='*' .venv/bin/uvicorn src.main:app --host 127.0.0.1 --port 8300
```

- 首次启动会自动建库（SQLite `apeadmin.db`）并跑完插件迁移 v0001→v0019 + seed
- 注意：全新库时 `sys_plugin` 里 apehub_web 默认 `enabled=0`，需手动启用一次：
  ```bash
  sqlite3 apeadmin.db "UPDATE sys_plugin SET enabled=1 WHERE name='apehub_web';"
  ```
  然后重启服务。之后的启停状态会自动持久化。
- 验证：`curl http://127.0.0.1:8300/apehub-web/index.html` 应返回 200

### 2. 插件代码开发

直接编辑 `backend/src/plugins/builtin/apehub_web/` 下的文件（api.py / models.py / static/ 等），保存后重启服务验证。

### 3. 提交与推送（关键：先看清楚在哪一层）

```bash
cd 官网demo/apeadmin-dev/backend/src/plugins/builtin/apehub_web
git status          # 确认只包含插件文件
git add <具体文件>   # 不要 git add -A 连带基座文件
git commit -m "..."
git -c http.proxy= -c https.proxy= push origin main   # GitCode 需绕过 Clash 代理
```

### 4. 前端管理后台（可选）

```bash
cd apeadmin-dev/frontend
npm install --legacy-peer-deps
npm run dev
```

## 环境备忘

- Python 3.12.13 + venv 位于 `backend/.venv/`
- 依赖清单：`backend/requirements.txt`（与线上生产版本一致，清华源安装）
- 本机 Clash 代理会劫持 127.0.0.1，curl/启动均需 `NO_PROXY='*'` 或 `-u http_proxy` 环境变量清空
- GitCode 推送用 `git -c http.proxy= -c https.proxy= push`
- GitHub 走 SSH（22 端口间歇超时，失败重试 1-2 次）

## 部署到线上

线上（apehub.finecv.cn）插件目录：`/www/wwwroot/apeadmin/src/plugins/builtin/apehub_web/`

```bash
# 单文件同步
scp <file> root@apehub.finecv.cn:/www/wwwroot/apeadmin/src/plugins/builtin/apehub_web/<path>

# 服务重启（改了 py 代码才需要）
ssh root@apehub.finecv.cn "systemctl restart apeadmin"
```

线上为 MySQL 5.7，本地为 SQLite——写迁移时注意双方言兼容（参考 v0018/v0019 的 dialect 分支写法）。

## 历史遗留（只读参考）

旧的插件开发工作区 `TeleAgent的工作空间/apeadmin/backend/src/plugins/builtin/apehub_web/` 仍然存在，
其 git 已同步到与远程一致（7b815c3）。后续开发一律用本目录，旧目录仅作参考，勿再改动。

2026-09-16